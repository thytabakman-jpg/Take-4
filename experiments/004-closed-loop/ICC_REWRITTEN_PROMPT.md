# ICC Rewritten Prompt — Closed-Loop Map Experiment

I own the continuation.

Use the entire M0-M11 mathematics map as the hypothesis space and the existing autonomous witness as the first fixture. Do not optimize one numbered region in isolation.

Build the smallest executable experimental loop that can produce inspectable evidence for:
SELECT -> BIND -> EXECUTE -> OBSERVE -> VERIFY -> UPDATE -> RESELECT.

The loop must distinguish semantic intent from executed transition, preserve target/authority/provenance/OPEN/INCOMPARABLE, expose an execution receipt, re-enter after failed verification, and make its trace usable as an ablation surface for the math map.

Do not build a production kernel. Do not touch Take-2. Do not promote any controller.

Run the fixture. Record the result.

Then ask me:
Why?
What's next?

I choose the continuation from the resulting evidence.
